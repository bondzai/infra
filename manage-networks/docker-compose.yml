version: "3"

services:
  pihole:
    container_name: pihole-con
    hostname: pihole
    image: ${PIHOLE_IMAGE}
    ports:
      - "1053:53/tcp"
      - "1053:53/udp"
      - "1067:67/udp"
      - "1080:80/tcp"
      - "1443:443/tcp"
    environment:
      TZ: 'Asia/Bangkok'
      WEBPASSWORD: 1234
    volumes:
      - './data/etc-pihole:/etc/pihole'
      - './data/etc-dnsmasq.d:/etc/dnsmasq.d'
    dns:
      - 127.0.0.1
      - 1.1.1.1
    cap_add:
      - NET_ADMIN
    restart: always

  # npm:
  #   image: ${NPM_IMAGE}
  #   container_name: proxy-manager-con
  #   hostname: proxy-manager
  #   restart: unless-stopped
  #   ports:
  #     - 280:80
  #     - 281:81
  #     - 2443:443
  #   volumes:
  #     - ./data:/data
  #     - ./letsencrypt:/etc/letsencrypt

  # wireshark:
  #   image: ${WIRESHARK_IMAGE}
  #   container_name: wireshark-con
  #   hostname: wireshark
  #   cap_add:
  #     - NET_ADMIN
  #   security_opt:
  #     - seccomp:unconfined #optional
  #   environment:
  #     - PUID=1000
  #     - PGID=1000
  #     - TZ=Etc/UTC
  #   volumes:
  #     - /path/to/config:/config
  #   ports:
  #     - 23000:3000 
  #     - 23001:3001
  #   restart: unless-stopped

volumes:
  data: