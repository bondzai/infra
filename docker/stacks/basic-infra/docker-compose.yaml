version: "3.8"

services:
  rabbitmq:
    extends:
      file: ../../configs/services.yaml
      service: rabbitmq
    networks:
      - backend

  redis:
    extends:
      file: ../configs/services.yaml
      service: redis
    networks:
      - backend

  redisinsight:
    extends:
      file: ../configs/services.yaml
      service: redisinsight
    networks:
      - backend

  postgres:
    extends:
      file: ../configs/services.yaml
      service: postgres
    networks:
      - backend

  pgadmin:
    extends:
      file: ../configs/services.yaml
      service: pgadmin
    networks:
      - backend

  influx:
    extends:
      file: ../configs/services.yaml
      service: influx
    volumes:
      - ./data/influx_backup:/var/lib/influxdb
      - .:/repo/
    networks:
      - backend

volumes:
  data:
  redisinsight-data:
networks:
  backend:
    driver: bridge
